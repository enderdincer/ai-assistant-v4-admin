# =============================================================================
# AI Assistant v4 Admin Dashboard - Frontend Configuration
# =============================================================================

# -----------------------------------------------------------------------------
# Backend Connection
# -----------------------------------------------------------------------------
# WebSocket URL for real-time updates from backend
# Development:
VITE_WS_URL=ws://localhost:3002
# Production:
# VITE_WS_URL=wss://assistant-admin-web.defterlab.com/api/ws

# REST API URL for backend (currently unused, reserved for future)
# Development:
VITE_API_URL=http://localhost:3001
# Production:
# VITE_API_URL=https://assistant-admin-web.defterlab.com/api

# -----------------------------------------------------------------------------
# WebSocket Reconnection Settings
# -----------------------------------------------------------------------------
# Initial delay in milliseconds before attempting to reconnect
VITE_WS_RECONNECT_DELAY=1000

# Maximum delay in milliseconds between reconnection attempts
# Uses exponential backoff: 1s -> 2s -> 4s -> 8s -> ... -> max
VITE_WS_MAX_RECONNECT_DELAY=30000
