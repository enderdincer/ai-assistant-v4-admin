# =============================================================================
# AI Assistant v4 Admin Dashboard - Environment Configuration
# =============================================================================
# Copy this file to .env and adjust values as needed.
# All values shown are defaults.
# =============================================================================

# -----------------------------------------------------------------------------
# General
# -----------------------------------------------------------------------------
# Environment mode (development, production)
NODE_ENV=production

# -----------------------------------------------------------------------------
# MQTT Broker (Mosquitto)
# -----------------------------------------------------------------------------
# MQTT broker port (exposed to host)
MQTT_PORT=1883

# MQTT WebSocket port (exposed to host, for browser-based MQTT clients)
MQTT_WS_PORT=9001

# -----------------------------------------------------------------------------
# Backend Service
# -----------------------------------------------------------------------------
# HTTP server port for REST API endpoints (exposed to host)
PORT=3001

# WebSocket server port for real-time frontend updates (exposed to host)
WS_PORT=3002

# Unique client ID for MQTT connection (must be unique per instance)
MQTT_CLIENT_ID=admin-dashboard-backend

# Time in milliseconds after which a service is marked as unhealthy
# if no health message is received (default: 60 seconds)
SERVICE_TIMEOUT_MS=60000

# Interval in milliseconds to check for service timeouts
# Lower values = faster detection but more CPU usage (default: 5 seconds)
HEALTH_CHECK_INTERVAL_MS=5000

# -----------------------------------------------------------------------------
# Frontend Service
# -----------------------------------------------------------------------------
# Port where the frontend will be accessible (exposed to host)
FRONTEND_PORT=8080

# WebSocket URL for the frontend to connect to the backend
# This should be accessible from the user's browser
VITE_WS_URL=ws://localhost:3002

# REST API URL for the frontend (currently unused, reserved for future)
VITE_API_URL=http://localhost:3001
